# A small implementation of Lustre in MLIR

## Building

First, modify the paths `DMLIR_DIR` and `DLLVM_EXTERNAL_LIT` in `setup.sh`.
Then, run `./setup.sh`.
Finally, run `./build.sh` to build the project.

